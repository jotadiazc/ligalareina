[project]
name = "liga-fixtures"
version = "0.1.0"
description = "Aplicación para administrar una liga de fútbol y generar fixtures automáticos con restricciones"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
